export interface FaqItem {
  question: string;
  answer: string;
}

export interface BenefitItem {
  before: string;
  after: string;
}

export interface PromptCategory {
  title: string;
  description: string;
  count: number;
}